application:
  runtimeManifest:
    packages:
      admin-ui-sdk:
        license: Apache-2.0
        actions:
          registration:
            $include: actions/registration/ext.config.yaml
      order-manager-app:
        license: Apache-2.0
        actions:
          $include: actions/massActions/ext.config.yaml
